{
  "name": "keystone-app",
  "version": "3.0.1",
  "private": true,
  "scripts": {
    "dev": "keystone-next dev",
    "start": "keystone-next start",
    "build": "keystone-next build",
    "deploy": "keystone-next build && keystone-next prisma migrate deploy",
    "seed-data": "keystone-next --seed-data",
    "postinstall": "keystone-next postinstall"
  },
  "build:docker": "keystone-next build && keystone-next prisma migrate deploy /cfg-cmd/ --outputPath=../../usr/share/nginx/html/cfg-cms",
  "dependencies": {
    "@keystone-next/auth": "^34.0.0",
    "@keystone-next/fields-document": "^11.0.0",
    "@keystone-next/keystone": "^26.1.0",
    "typescript": "^4.4.3"
  }
}
